# MG-Games Safe Area 가이드

> **문서 버전**: 1.0.0
> **최종 수정**: 2025-12-19
> **적용 대상**: 52종 전체 게임 (MG-0001 ~ MG-0052)

---

## 1. 개요

### 1.1 목적
다양한 기기의 노치, 펀치홀, 다이나믹 아일랜드, 제스처 바 등을 고려한 안전 영역(Safe Area) 가이드라인을 정의합니다.

### 1.2 Safe Area란?

```
┌────────────────────────────────────┐
│▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓│ ← 시스템 UI (상태 바, 노치)
├────────────────────────────────────┤
│                                    │
│                                    │
│           SAFE AREA                │ ← 콘텐츠 안전 영역
│     (UI/콘텐츠 배치 가능)           │
│                                    │
│                                    │
├────────────────────────────────────┤
│▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓│ ← 시스템 UI (네비게이션, 제스처)
└────────────────────────────────────┘
```

**Safe Area**: 시스템 UI 요소(노치, 상태바, 제스처 바)와 겹치지 않는 안전한 콘텐츠 영역

---

## 2. 기기별 Safe Area 스펙

### 2.1 iOS 기기

#### iPhone (노치 시리즈)
| 모델 | 상단 | 하단 | 좌우 |
|------|------|------|------|
| iPhone X/XS/11 Pro | 44pt | 34pt | 0pt |
| iPhone XR/11 | 48pt | 34pt | 0pt |
| iPhone 12/13/14 | 47pt | 34pt | 0pt |
| iPhone 12/13/14 Pro | 59pt | 34pt | 0pt |

#### iPhone (Dynamic Island)
| 모델 | 상단 | 하단 | 좌우 |
|------|------|------|------|
| iPhone 14 Pro/Pro Max | 59pt | 34pt | 0pt |
| iPhone 15/15 Pro | 59pt | 34pt | 0pt |
| iPhone 16/16 Pro | 62pt | 34pt | 0pt |

#### iPhone (비노치)
| 모델 | 상단 | 하단 | 좌우 |
|------|------|------|------|
| iPhone SE (2/3세대) | 20pt | 0pt | 0pt |
| iPhone 8 이하 | 20pt | 0pt | 0pt |

#### iPad
| 모델 | 상단 | 하단 | 좌우 |
|------|------|------|------|
| iPad (홈버튼) | 20pt | 0pt | 0pt |
| iPad Pro (Face ID) | 24pt | 20pt | 0pt |

### 2.2 Android 기기

#### 노치/펀치홀 유형

```
물방울 노치          펀치홀 (중앙)        펀치홀 (좌측)
┌─────⌒─────┐       ┌─────●─────┐       ┌●────────┐
│           │       │           │       │         │
│           │       │           │       │         │
└───────────┘       └───────────┘       └─────────┘
  24-32dp             24-32dp            24-32dp
```

#### 일반적인 Android Safe Area
| 요소 | 최소 | 일반 | 최대 |
|------|------|------|------|
| 상태바 | 24dp | 24-32dp | 48dp |
| 노치/펀치홀 | 0dp | 24-32dp | 48dp |
| 제스처 바 | 0dp | 48dp | 96dp |
| 3버튼 내비게이션 | 48dp | 48dp | 56dp |

#### 제조사별 특이사항

| 제조사 | 특이사항 |
|--------|----------|
| Samsung | 에지 스크린: 좌우 8dp 추가 여백 권장 |
| Xiaomi | 상단 알림바 높이 변동 가능 (24-36dp) |
| OPPO/Vivo | 둥근 모서리: 16dp 코너 여백 권장 |
| Huawei | 노치 높이 변동 (24-44dp) |

---

## 3. Flutter Safe Area 구현

### 3.1 기본 SafeArea 사용

```dart
// 전체 화면 Safe Area 적용
@override
Widget build(BuildContext context) {
  return Scaffold(
    body: SafeArea(
      child: GameContent(),
    ),
  );
}
```

### 3.2 선택적 Safe Area 적용

```dart
// 특정 방향만 Safe Area 적용
SafeArea(
  top: true,      // 상단 (노치, 상태바)
  bottom: true,   // 하단 (제스처 바)
  left: false,    // 좌측
  right: false,   // 우측
  child: content,
)
```

### 3.3 커스텀 패딩으로 Safe Area

```dart
// MediaQuery로 정확한 인셋 가져오기
@override
Widget build(BuildContext context) {
  final padding = MediaQuery.of(context).padding;
  final viewInsets = MediaQuery.of(context).viewInsets;

  return Container(
    padding: EdgeInsets.only(
      top: padding.top,
      bottom: max(padding.bottom, viewInsets.bottom), // 키보드 고려
      left: padding.left,
      right: padding.right,
    ),
    child: GameContent(),
  );
}
```

### 3.4 게임 영역과 UI 영역 분리

```dart
class GameScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    final padding = MediaQuery.of(context).padding;

    return Stack(
      children: [
        // 게임 영역: Safe Area 무시 (풀스크린)
        Positioned.fill(
          child: GameCanvas(),
        ),

        // HUD: Safe Area 적용
        Positioned(
          top: padding.top + 8,
          left: 16,
          right: 16,
          child: GameHUD(),
        ),

        // 액션 버튼: Safe Area 적용
        Positioned(
          bottom: padding.bottom + 16,
          left: 16,
          right: 16,
          child: ActionButtons(),
        ),
      ],
    );
  }
}
```

---

## 4. 게임 티어별 Safe Area 전략

### 4.1 Tier 1: Simple (캐주얼)

```
┌────────────────────────────────────┐
│  ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │ ← 상태바 영역 (투명/반투명)
├────────────────────────────────────┤
│  [점수: 1,234]        [⚙️]        │ ← HUD (Safe Area 내부)
│                                    │
│                                    │
│         ████████████               │ ← 게임 영역 (풀스크린)
│         ██ 게임 ████               │
│         ████████████               │
│                                    │
│                                    │
│           [PLAY]                   │ ← CTA (Safe Area 내부)
├────────────────────────────────────┤
│  ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │ ← 제스처 바 영역
└────────────────────────────────────┘

전략:
✓ 게임 배경: 풀스크린 (Safe Area 무시)
✓ HUD/버튼: Safe Area 내부에 배치
✓ 상단: 최소 44dp 여백
✓ 하단: 최소 34dp 여백
```

### 4.2 Tier 2: Medium (미드코어)

```
┌────────────────────────────────────┐
│  ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │
├────────────────────────────────────┤
│  [LV.25] [💰1.2K] [💎50]  [⚙️][📬]│ ← 리소스 바
├────────────────────────────────────┤
│                                    │
│           게임 영역                 │
│                                    │
├────────────────────────────────────┤
│  [스킬1] [스킬2] [스킬3] [궁]      │ ← 액션 바
├────────────────────────────────────┤
│  [홈] [퀘스트] [상점] [인벤] [소셜]│ ← 네비게이션 바
├────────────────────────────────────┤
│  ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │
└────────────────────────────────────┘

전략:
✓ 리소스 바: 상단 Safe Area 직후
✓ 네비게이션: 하단 Safe Area 직전
✓ 게임 영역: 중앙 최대화
```

### 4.3 Tier 3: Complex (Level A JRPG)

```
┌────────────────────────────────────┐
│  ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │
├────────────────────────────────────┤
│  [HP████░] [MP████] [버프] [⚙️]   │ ← 상태 바
├────────────────────────────────────┤
│                                    │
│  ┌─────────────────────────────┐   │
│  │                             │   │
│  │       전투 영역              │   │ ← 메인 게임 영역
│  │       (풀스크린)             │   │
│  │                             │   │
│  └─────────────────────────────┘   │
│                                    │
├────────────────────────────────────┤
│  [P1░░] [P2░░] [P3░░] [P4░░]      │ ← 파티 상태
├────────────────────────────────────┤
│  [공격] [스킬] [아이템] [방어]     │ ← 커맨드 메뉴
├────────────────────────────────────┤
│  ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │
└────────────────────────────────────┘

전략:
✓ 다중 HUD 레이어: Safe Area 내부
✓ 전투 애니메이션: 풀스크린 활용
✓ 커맨드 메뉴: 터치 영역 확보 필수
```

---

## 5. Landscape 게임 Safe Area

### 5.1 가로 모드 특수 처리

```
┌──────────────────────────────────────────────────────────┐
│░░│                                                    │░░│
│░░│  [HP]                              [AMMO] [미니맵] │░░│
│░░├────────────────────────────────────────────────────│░░│
│░░│                                                    │░░│
│░░│                   게임 영역                         │░░│
│░░│                  (16:9 영역)                       │░░│
│░░│                                                    │░░│
│░░├────────────────────────────────────────────────────│░░│
│░░│  [조준]                              [사격] [스킬] │░░│
│░░│                                                    │░░│
└──────────────────────────────────────────────────────────┘
 │                                                      │
 └─ 좌측 Safe Area (노치/펀치홀)        우측 Safe Area ─┘
```

### 5.2 Landscape Safe Area 코드

```dart
class LandscapeGameScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    final padding = MediaQuery.of(context).padding;

    return Row(
      children: [
        // 좌측 Safe Area
        SizedBox(width: padding.left),

        // 메인 게임 영역
        Expanded(
          child: Column(
            children: [
              // 상단 HUD
              Padding(
                padding: EdgeInsets.only(top: padding.top + 8),
                child: TopHUD(),
              ),

              // 게임 영역
              Expanded(child: GameCanvas()),

              // 하단 컨트롤
              Padding(
                padding: EdgeInsets.only(bottom: padding.bottom + 8),
                child: BottomControls(),
              ),
            ],
          ),
        ),

        // 우측 Safe Area
        SizedBox(width: padding.right),
      ],
    );
  }
}
```

---

## 6. 특수 기기 대응

### 6.1 폴더블 기기

```dart
// 폴더블 기기 감지 및 대응
class FoldableSafeArea extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    final size = MediaQuery.of(context).size;
    final displayFeatures = MediaQuery.of(context).displayFeatures;

    // 접힌 부분(힌지) 감지
    final hinge = displayFeatures.whereType<DisplayFeature>()
        .where((f) => f.type == DisplayFeatureType.hinge)
        .firstOrNull;

    if (hinge != null) {
      // 폴더블 펼침 상태: 힌지 영역 피하기
      return Row(
        children: [
          Expanded(child: LeftPanel()),
          SizedBox(width: hinge.bounds.width),  // 힌지 영역
          Expanded(child: RightPanel()),
        ],
      );
    }

    return StandardLayout();
  }
}
```

### 6.2 둥근 모서리 대응

```dart
// 둥근 모서리 영역 확보
class RoundedCornerSafeArea extends StatelessWidget {
  // 일반적인 둥근 모서리 반경
  static const double cornerRadius = 24.0;

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: EdgeInsets.all(8),  // 모서리 여백
      child: ClipRRect(
        borderRadius: BorderRadius.circular(cornerRadius),
        child: GameContent(),
      ),
    );
  }
}
```

### 6.3 카메라 컷아웃 대응

```dart
// 카메라 컷아웃(펀치홀) 영역 확보
Widget buildWithCutoutAvoidance(BuildContext context) {
  final padding = MediaQuery.of(context).padding;
  final viewPadding = MediaQuery.of(context).viewPadding;

  // viewPadding은 시스템 UI 포함
  // padding은 현재 SafeArea가 적용된 상태
  final cutoutTop = viewPadding.top;

  return Container(
    margin: EdgeInsets.only(top: cutoutTop),
    child: GameContent(),
  );
}
```

---

## 7. 게임 요소별 Safe Area 적용

### 7.1 항상 Safe Area 내부에 배치

| 요소 | 이유 |
|------|------|
| HUD (점수, HP, 자원) | 항상 보여야 함 |
| 버튼 (CTA, 스킬) | 터치 가능해야 함 |
| 텍스트 (대화, 알림) | 읽을 수 있어야 함 |
| 팝업/모달 | 전체 내용 표시 필요 |
| 설정/메뉴 | 조작 가능해야 함 |

### 7.2 Safe Area 무시 가능

| 요소 | 이유 |
|------|------|
| 배경 이미지 | 시각적 몰입감 |
| 풀스크린 애니메이션 | 연출 효과 |
| 그라데이션/비네팅 | 시각적 효과 |
| 파티클 효과 | 시각적 효과 |

### 7.3 조건부 Safe Area

```dart
// 게임 상태에 따른 Safe Area 적용
Widget buildDynamicSafeArea(BuildContext context, GameState state) {
  final padding = MediaQuery.of(context).padding;

  if (state.isInCutscene) {
    // 컷씬: 풀스크린 (자막만 Safe Area)
    return Stack(
      children: [
        Positioned.fill(child: CutscenePlayer()),
        Positioned(
          bottom: padding.bottom + 16,
          left: 16,
          right: 16,
          child: Subtitles(),
        ),
      ],
    );
  }

  if (state.isInBattle) {
    // 전투: 게임은 풀스크린, UI는 Safe Area
    return Stack(
      children: [
        Positioned.fill(child: BattleCanvas()),
        SafeArea(child: BattleHUD()),
      ],
    );
  }

  // 로비: 전체 Safe Area
  return SafeArea(child: LobbyScreen());
}
```

---

## 8. 권장 여백 값

### 8.1 최소 여백 (dp)

| 영역 | Portrait | Landscape |
|------|----------|-----------|
| 상단 (노치 없음) | 24 | 0 |
| 상단 (노치/Dynamic Island) | 44-62 | 0 |
| 하단 (제스처 바) | 34-48 | 0 |
| 하단 (버튼 네비게이션) | 48-56 | 0 |
| 좌측 (Landscape) | 0 | 24-44 |
| 우측 (Landscape) | 0 | 24-44 |

### 8.2 권장 추가 여백

```dart
class MGSafeAreaConfig {
  // HUD와 Safe Area 경계 사이 여백
  static const double hudMargin = 8.0;

  // 터치 버튼과 Safe Area 경계 사이 여백
  static const double buttonMargin = 16.0;

  // 텍스트와 Safe Area 경계 사이 여백
  static const double textMargin = 16.0;

  // 모달과 Safe Area 경계 사이 여백
  static const double modalMargin = 24.0;
}
```

---

## 9. 공통 Safe Area 위젯

### 9.1 MGSafeArea 위젯

```dart
/// MG-Games 커스텀 Safe Area 위젯
class MGSafeArea extends StatelessWidget {
  final Widget child;
  final bool top;
  final bool bottom;
  final bool left;
  final bool right;
  final double topPadding;
  final double bottomPadding;

  const MGSafeArea({
    required this.child,
    this.top = true,
    this.bottom = true,
    this.left = true,
    this.right = true,
    this.topPadding = 8.0,
    this.bottomPadding = 8.0,
  });

  @override
  Widget build(BuildContext context) {
    final padding = MediaQuery.of(context).padding;

    return Padding(
      padding: EdgeInsets.only(
        top: top ? padding.top + topPadding : 0,
        bottom: bottom ? padding.bottom + bottomPadding : 0,
        left: left ? padding.left : 0,
        right: right ? padding.right : 0,
      ),
      child: child,
    );
  }
}
```

### 9.2 MGGameCanvas 위젯

```dart
/// 게임 캔버스 (풀스크린) + HUD (Safe Area)
class MGGameCanvas extends StatelessWidget {
  final Widget gameLayer;
  final Widget? topHud;
  final Widget? bottomHud;
  final Widget? leftHud;
  final Widget? rightHud;

  const MGGameCanvas({
    required this.gameLayer,
    this.topHud,
    this.bottomHud,
    this.leftHud,
    this.rightHud,
  });

  @override
  Widget build(BuildContext context) {
    final padding = MediaQuery.of(context).padding;

    return Stack(
      children: [
        // 게임 레이어 (풀스크린)
        Positioned.fill(child: gameLayer),

        // 상단 HUD
        if (topHud != null)
          Positioned(
            top: padding.top + 8,
            left: padding.left + 16,
            right: padding.right + 16,
            child: topHud!,
          ),

        // 하단 HUD
        if (bottomHud != null)
          Positioned(
            bottom: padding.bottom + 16,
            left: padding.left + 16,
            right: padding.right + 16,
            child: bottomHud!,
          ),

        // 좌측 HUD (Landscape)
        if (leftHud != null)
          Positioned(
            top: padding.top + 16,
            bottom: padding.bottom + 16,
            left: padding.left + 8,
            child: leftHud!,
          ),

        // 우측 HUD (Landscape)
        if (rightHud != null)
          Positioned(
            top: padding.top + 16,
            bottom: padding.bottom + 16,
            right: padding.right + 8,
            child: rightHud!,
          ),
      ],
    );
  }
}
```

---

## 10. 테스트 체크리스트

### 10.1 기기별 테스트

```
iOS 테스트:
[ ] iPhone SE (홈버튼) - 상단 20pt
[ ] iPhone 12/13 (노치) - 상단 47pt, 하단 34pt
[ ] iPhone 14 Pro (Dynamic Island) - 상단 59pt, 하단 34pt
[ ] iPhone 16 Pro - 상단 62pt, 하단 34pt
[ ] iPad Pro (Face ID) - 상단 24pt, 하단 20pt

Android 테스트:
[ ] Samsung Galaxy S (펀치홀 중앙) - 상단 28dp
[ ] Samsung Galaxy Z Fold (폴더블) - 힌지 영역 대응
[ ] Xiaomi (물방울 노치) - 상단 32dp
[ ] Pixel (펀치홀 좌측) - 상단 28dp
[ ] 저가형 기기 (3버튼 내비게이션) - 하단 48dp
```

### 10.2 UI 요소 테스트

```
[ ] HUD가 노치/펀치홀에 가려지지 않는가?
[ ] 버튼이 제스처 바와 겹치지 않는가?
[ ] 텍스트가 Safe Area 내부에서 읽히는가?
[ ] 모달이 Safe Area 내부에 완전히 표시되는가?
[ ] 풀스크린 요소가 화면 전체를 채우는가?
[ ] 둥근 모서리에서 콘텐츠가 잘리지 않는가?
```

---

## 11. 버전 히스토리

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| 1.0.0 | 2025-12-19 | 초기 문서 작성 |

---

*이 문서는 MG-Games 52종 게임의 Safe Area 처리를 정의합니다.*
